# App components
There are 12 components and each component corresponds to a page.

The route rules are saved on the app.routes.ts.

The CSS is imported in the style.css file. The index.html applies all the CSS rules. I didn't separate each CSS rule to each component.

